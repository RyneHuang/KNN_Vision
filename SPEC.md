# KNN算法可视化教学演示系统 - 需求规格说明书

## 1. 项目概述

### 1.1 项目名称
KNN算法可视化教学演示系统（KNN Vision）

### 1.2 项目类型
纯前端单HTML文件应用

### 1.3 核心功能
通过交互式可视化方式演示K近邻（K-Nearest Neighbors）算法的工作原理，帮助用户直观理解算法机制。

### 1.4 目标用户
- 机器学习初学者
- 教学场景中的教师和学生
- 对KNN算法感兴趣的技术人员

---

## 2. 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | 纯HTML/CSS/JavaScript（单文件） |
| 可视化库 | Plotly.js |
| 数据集 | 内置模拟数据 |

---

## 3. 内置数据集

系统提供4个内置数据集，用户可通过下拉菜单选择：

### 3.1 乳腺癌诊断数据集（Breast Cancer）
- 样本数量：569
- 特征维度：2（从30维中选取2个代表性特征）
- 类别：恶性（Malignant）、良性（Benign）
- 用途：二分类经典问题

### 3.2 鸢尾花数据集（Iris）
- 样本数量：150
- 特征维度：2（从4维中选取2个代表性特征：花萼长度、花瓣长度）
- 类别：3种鸢尾花（Setosa、Versicolor、Virginica）
- 说明：教学中仅使用前两个类别便于可视化

### 3.3 月亮数据集（Moon）
- 样本数量：200
- 特征维度：2
- 类别：2个月牙形簇
- 特点：非线性和可分离

### 3.4 同心圆数据集（Circle）
- 样本数量：200
- 特征维度：2
- 类别：2个同心圆环
- 特点：非线性不可分

---

## 4. 数据归一化

### 4.1 归一化开关
- 提供Min-Max归一化开关（默认关闭）
- 用户可选择是否对数据进行归一化处理

### 4.2 归一化公式
```
X_scaled = (X - X_min) / (X_max - X_min)
```
归一化后数据范围：[0, 1]

### 4.3 归一化影响
- 归一化开启时：距离计算基于归一化后的数据
- 归一化关闭时：距离计算基于原始数据

---

## 5. KNN算法参数

### 5.1 K值设置
- 范围：1-30（整数）
- 默认值：5
- 用户通过滑块或输入框调整

### 5.2 距离度量
- 默认使用欧几里得距离
- 距离计算公式：
```
d(x, y) = sqrt(sum((x_i - y_i)^2))
```

### 5.3 决策边界
- 网格密度：50x50
- 网格范围：覆盖所有训练数据点的特征空间
- 决策边界根据KNN预测结果绘制

---

## 6. 交互功能

### 6.1 点击生成待测点
- 用户在主画布上点击任意位置
- 系统自动将该位置作为待测点（Test Point）
- 待测点以特殊样式标记（绿色）

### 6.2 显示K个最近邻居
- 点击待测点后
- 系统计算出K个最近邻点
- 以特殊连线标记待测点与K个邻居的连接
- 邻居点高亮显示

### 6.3 投票结果展示
- 显示K个邻居的类别投票统计
- 各类别的票数可视化（柱状图或数字）
- 显示最终预测结果

### 6.4 清除待测点
- 提供清除按钮移除待测点
- 清除后画布恢复初始状态

---

## 7. 布局结构

### 7.1 整体布局
```
+------------------------------------------+
|                  标题                      |
+---------------------------+--------------+
|                           |   控制面板    |
|                           |   - 数据集    |
|        主画布（70%）       |   - K值      |
|                           |   - 归一化   |
|                           |   - 清除按钮  |
|                           |   - 投票结果  |
+---------------------------+--------------+
```

### 7.2 左侧主画布（70%宽度）
- 使用Plotly.js绑定的div容器
- 显示数据集散点图
- 显示决策边界热力图
- 支持点击事件监听

### 7.3 右侧控制面板（30%宽度）
- 数据集选择下拉菜单
- K值调节滑块（1-30）
- 归一化开关（checkbox）
- 清除待测点按钮
- 投票结果显示区域

---

## 8. 配色方案

### 8.1 类别配色
| 类别 | 颜色 | 说明 |
|------|------|------|
| 类别1（负类） | 浅红色（#FF6B6B） | 数据点填充 |
| 类别2（正类） | 浅蓝色（#4ECDC4） | 数据点填充 |

### 8.2 待测点配色
| 元素 | 颜色 | 说明 |
|------|------|------|
| 待测点 | 绿色（#2ECC71） | 标记待分类点 |

### 8.3 邻居连线
- 灰色虚线（#888888）
- 线宽：1-2px

### 8.4 决策边界
- 渐变色或半透明填充
- 反映各类别预测概率

---

## 9. 函数设计

### 9.1 核心函数

#### generateDataset(datasetName)
- 输入：数据集名称
- 输出：包含X、y的数据对象
- 功能：根据名称生成对应数据集

#### normalizeData(X)
- 输入：原始特征矩阵
- 输出：归一化后的特征矩阵
- 功能：Min-Max归一化

#### knn_predict(X_train, y_train, X_test, k)
- 输入：训练数据、训练标签、测试点、K值
- 输出：预测类别
- 功能：KNN分类预测

#### getKNeighbors(X_train, y_train, X_test, k)
- 输入：训练数据、测试点、K值
- 输出：K个最近邻的索引和距离
- 功能：查找K个最近邻

#### plotDecisionBoundary(X, y, k, normalize)
- 输入：数据、参数
- 输出：无（直接绑定到Plotly图表）
- 功能：绘制50x50网格决策边界

#### handleCanvasClick(event)
- 输入：点击事件
- 输出：无
- 功能：处理画布点击，生成待测点

---

## 10. 用户操作流程

### 10.1 初始化
1. 页面加载完成
2. 默认显示乳腺癌数据集
3. K值默认为5
4. 归一化默认关闭
5. 绘制初始散点图

### 10.2 切换数据集
1. 用户从下拉菜单选择数据集
2. 系统重新生成数据
3. 重新绘制散点图和决策边界
4. 清除现有待测点（如有）

### 10.3 调整K值
1. 用户拖动滑块或输入K值
2. 系统重新计算决策边界
3. 决策边界实时更新

### 10.4 开关归一化
1. 用户切换归一化开关
2. 如开启：对原始数据进行归一化
3. 重新计算决策边界并更新

### 10.5 点击生成待测点
1. 用户在画布上点击
2. 系统计算该点的K个最近邻
3. 在图上标记待测点
4. 用连线连接待测点和K个邻居
5. 显示投票结果

### 10.6 清除待测点
1. 用户点击清除按钮
2. 系统移除待测点和相关连线
3. 画布恢复到仅显示数据集状态

---

## 11. 性能要求

### 11.1 响应速度
- 数据集切换：< 500ms
- K值调整：< 200ms
- 点击响应：< 100ms

### 11.2 决策边界计算
- 网格大小：50x50 = 2500个点
- 单次预测需计算2500个点的类别
- 应保证流畅的用户体验

---

## 12. 浏览器兼容性

- Chrome/Edge：最新版本
- Firefox：最新版本
- Safari：最新版本
- 无需支持IE浏览器

---

## 13. 文件结构

```
/Users/rynehuang/Documents/KNN_Vision/
├── index.html      # 单文件应用（HTML+CSS+JS）
├── SPEC.md         # 需求规格文档
├── .gitignore      # Git忽略配置
└── .git/           # Git仓库
```

---

## 14. 验收标准

### 14.1 功能验收
- [ ] 4个内置数据集可正常切换
- [ ] K值范围1-30可调
- [ ] 归一化开关功能正常
- [ ] 50x50决策边界正确显示
- [ ] 点击画布可生成待测点
- [ ] K个最近邻居正确显示
- [ ] 投票结果正确计算和显示
- [ ] 清除按钮功能正常

### 14.2 视觉验收
- [ ] 布局为左侧70%画布、右侧30%控制面板
- [ ] 类别1使用浅红色，类别2使用浅蓝色
- [ ] 待测点使用绿色标记
- [ ] 界面美观整洁

### 14.3 技术验收
- [ ] 纯前端单HTML文件
- [ ] 使用Plotly.js进行可视化
- [ ] 无需后端服务器
- [ ] 可直接在浏览器中打开运行
