# Welcome to ColaMD

KNN算法可视化教学演示系统需求文档

## 项目概述

本项目旨在开发一款基于Web的单页面应用程序，用于辅助高职学生直观理解K-近邻（KNN）算法的核心原理。系统需完全运行于浏览器端，无需后端服务器支持，通过JavaScript实现数据归一化、距离计算及决策边界绘制的全流程。核心目标是让学生通过交互操作，深刻理解数据尺度对距离计算的影响以及K值对模型复杂度的调节作用。

## 技术架构与运行环境

### 技术栈要求

* **核心语言**: HTML5, CSS3, JavaScript (ES6+)
* **可视化库**: 推荐使用Plotly.js或D3.js
* **数据处理**: 原生JavaScript实现
* **算法实现**: 手写KNN核心逻辑

### 运行环境

* 纯前端运行，无需后端服务器
* 支持主流现代浏览器（Chrome, Firefox, Edge, Safari）
* 响应式设计，适配常见笔记本屏幕分辨率

### 交付标准

* 单一HTML文件或简单静态文件包
* 双击即可运行，无需复杂配置
* 关键算法部分包含清晰中文注释

## 数据集管理模块

### 内置经典数据集

系统需内置以下经典数据集，通过下拉菜单供用户切换：

#### 1. 威斯康星州乳腺癌诊断数据集

* **特征维度**: 平均半径、平均平滑度
* **数值特点**: 平均半径数值较大，平均平滑度数值较小
* **教学目的**: 极佳展示未归一化数据在距离计算中的尺度偏差问题
* **分类任务**: 良性 vs 恶性肿瘤

#### 2. 鸢尾花数据集 (Iris Dataset)

* **特征维度**: 花萼长度、花萼宽度
* **数据特点**: 线性可分与近似线性可分
* **教学目的**: 展示基础分类场景
* **分类任务**: 三种鸢尾花分类

#### 3. 月亮数据集 (Moons Dataset)

* **数据特点**: 非线性可分的月牙形分布
* **教学目的**: 展示KNN在处理非凸形状数据分布时的优势

#### 4. 同心圆数据集 (Circles Dataset)

* **数据特点**: 复杂的空间分布
* **教学目的**: 验证KNN算法在局部邻域分类中的表现

### 数据集切换功能

* 下拉菜单选择数据集
* 自动加载对应数据和配置
* 重置相关参数和可视化状态

## 核心功能模块

### 1. 数据预处理与归一化可视化

#### 功能要求

* 提供"启用Min-Max归一化"开关
* 实时切换原始数据和归一化数据展示
* 动态刷新图表显示效果

#### 视觉效果

* **未归一化状态**:
  * 坐标轴刻度差异巨大
  * 数据点视觉上压缩成一条线
  * 展示尺度偏差问题
* **归一化状态**:
  * 特征值映射至\[0,1]区间
  * 坐标轴同步调整
  * 数据点分布均匀，簇状结构清晰

#### 算法实现

* Min-Max归一化公式：`X_scaled = (X - X_min) / (X_max - X_min)`
* 实时计算和应用

### 2. K值调节与决策边界渲染

#### 参数设置

* K值滑块范围：1至30
* 实时显示当前K值
* 支持鼠标拖动和键盘调节

#### 决策边界绘制

* **网格法实现**:
  * 在画布区域生成50x50分辨率网格
  * 对每个网格点进行KNN预测
* **可视化方式**:
  * 半透明色块区分不同类别区域
  * 浅红色表示类别1，浅蓝色表示类别2
  * 平滑过渡显示决策边界

#### K值影响展示

* **小K值(K=1)**: 细碎、复杂的过拟合边界
* **中等K值**: 适中的边界复杂度
* **大K值(K=20+)**: 平滑、简单的欠拟合边界

### 3. 交互式预测与过程演示

#### 交互功能

* 画布点击生成待测样本点
* 实时计算距离和分类结果
* 动态视觉反馈

#### 预测流程可视化

1. **标记待测点**: 点击位置显示醒目绿色星号
2. **计算距离**: 计算至所有训练样本的欧氏距离
3. **绘制连线**: 连接最近的K个邻居
4. **高亮邻居**: 放大或加粗显示K个最近邻居

#### 结果展示

* 侧边栏显示详细计算过程
* 包含最近邻居坐标和距离值
* 显示最终类别投票统计结果

## 算法核心逻辑要求

### 距离计算

* 欧氏距离公式：`d = sqrt((x1-x2)² + (y1-y2)²)`
* 支持多维特征计算
* 实时性能优化

### KNN算法实现

* 手写距离计算和排序逻辑
* K近邻选择算法
* 多数投票分类决策

### 归一化算法

* Min-Max归一化实现
* 支持多特征独立归一化
* 反归一化功能（用于结果解释）

## 界面布局与交互设计

### 整体布局

采用现代化左右分栏布局：

* **左侧(70%)**: 主可视化画布
* **右侧(30%)**: 控制面板

### 主画布功能

* 显示数据点散点图
* 渲染决策边界背景
* 动态绘制预测连线
* 支持缩放和平移操作

### 控制面板布局

从上至下依次排列：

1. **数据集选择**: 下拉菜单
2. **归一化控制**: 开关按钮
3. **K值调节**: 滑块及数值显示
4. **预测结果**: 详情卡片

### 视觉设计规范

* 简洁学术风格
* 高对比度配色
* 清晰易读字体
* 适当动画效果
* 响应式布局

## 性能与兼容性要求

### 性能优化

* 实时交互响应（<200ms延迟）
* 网格计算优化
* 动画流畅性保证
* 内存使用控制

### 兼容性

* 支持Chrome、Firefox、Edge、Safari
* 适配Windows、macOS、Linux
* 移动端基础支持

## 教学功能扩展

### 学习引导

* 内置教学说明面板
* 关键概念解释
* 操作指引提示

### 实验记录

* 支持保存实验配置
* 导出结果图像
* 生成实验报告

### 评估功能

* 预测准确率计算
* 交叉验证支持
* 模型性能对比

## 交付物要求

### 文件结构

* 单一HTML文件或简单文件包
* 内嵌CSS样式和JavaScript逻辑
* 无需外部依赖或复杂配置

### 代码规范

* 关键算法部分包含中文注释
* 模块化代码结构
* 清晰的函数命名
* 适当的代码组织

### 文档要求

* 内置使用说明
* 算法原理简介
* 教学指导建议

## 测试用例

### 功能测试

* 数据集切换功能验证
* 归一化开关效果测试
* K值调节响应测试
* 预测交互功能测试

### 算法验证

* 距离计算准确性验证
* 归一化结果正确性验证
* 分类结果一致性验证

### 性能测试

* 大数据集加载性能
* 实时交互流畅性
* 内存使用情况监控

### 兼容性测试

* 多浏览器兼容性
* 多平台运行测试
* 不同分辨率适配

## 项目时间计划

### 阶段划分

1. **需求分析与设计** (1周)
2. **核心算法实现** (2周)
3. **界面开发与集成** (2周)
4. **测试与优化** (1周)
5. **文档编写与交付** (1周)

### 里程碑

* 算法核心逻辑完成
* 主要界面功能实现
* 系统集成测试通过
* 最终交付版本完成

## 风险评估与应对

### 技术风险

* **实时性能问题**: 优化算法实现，采用Web Worker
* **兼容性问题**: 充分测试，提供降级方案
* **算法准确性**: 严格验证，对比标准实现

### 应对措施

* 分阶段开发，及时验证核心功能
* 采用模块化设计，便于维护和扩展
* 建立完善的测试用例体系

## 后续扩展计划

### 短期扩展

* 增加更多经典数据集
* 完善教学引导功能
* 增强结果可视化效果

### 长期规划

* 支持多维数据可视化
* 增加其他距离度量方式
* 实现更复杂的机器学习算法

***

**项目负责人**: \[待填写]\
**开发团队**: \[待填写]\
**计划开始时间**: \[待填写]\
**预计完成时间**: \[待填写]

***

*本需求文档定义了KNN算法可视化教学演示系统的完整功能要求，为开发工作提供了明确的指导方向。*
